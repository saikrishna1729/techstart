---
/**
 * BaseLayout Component
 *
 * The main layout wrapper for all pages. Includes:
 * - Common <head> elements
 * - Header component
 * - Main content area
 * - Footer component
 * - Client-side scripts for consistency
 */

import "../styles/global.css";
import { SITE } from "../utils/site-config";
import Footer from "./Footer.astro";
import Header from "./Header.astro";

// Get props with defaults from site config
const {
	title = SITE.title,
	description = SITE.description,
	image = "/images/default-social.jpg",
	canonicalURL = Astro.url,
} = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content={description} />

		<!-- Canonical URL -->
		<link rel="canonical" href={canonicalURL} />

		<!-- Open Graph / Social Media Meta Tags -->
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:url" content={canonicalURL} />
		<meta property="og:image" content={new URL(image, SITE.url)} />
		<meta property="og:type" content="website" />
		<meta property="og:site_name" content={SITE.name} />

		<!-- Twitter Card -->
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />

		<title>{title}</title>
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />

		<!-- Footer override script to ensure consistent branding -->
		<script is:inline src="/scripts/footer-override.js"></script>
	</head>
	<body
		class="flex flex-col min-h-screen bg-gray-100 dark:bg-gray-800 text-gray-800 dark:text-gray-200 font-sans transition-all duration-300 opacity-0 animate-fade-in"
	>
		<Header />
		<main class="flex-grow container mx-auto px-8 py-12 md:px-12">
			<slot />
		</main>
		<Footer />
        <style>
            @keyframes fadeIn {
                from {
                    opacity: 0;
                }
                to {
                    opacity: 1;
                }
            }
            .animate-fade-in {
                animation: fadeIn 0.5s ease-in-out forwards;
            }
        </style>
	</body>
</html>
